- pipeline:
    name: check
    manager: independent
    trigger:
      github:
        - event:
            - pr-open
            - pr-change
            - pr-reopen

- job:
    name: project-test1
- job:
    name: project-test2

- project:
    name: org/project
    check:
      jobs:
        - project-test1
        - project-test2
