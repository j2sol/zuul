- pipeline:
    name: reviews
    manager: independent
    trigger:
      github:
        - event: pull_request
          action: review
          state: 'approve'
    success:
      github:
        label:
          - 'tests passed'

- job:
    name: project-reviews

- project:
    name: org/project
    reviews:
      jobs:
        - project-reviews
